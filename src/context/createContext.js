import {  createContext } from "react";

const CreateContext=createContext();
//this is the  method for createcontext and assigned the veriable
//useContext is the hoo which is use for state management 

export default CreateContext;